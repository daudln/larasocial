<div class="flex flex-col">
    <label for="{{ $name }}">{{ $label }}</label>
    <textarea
        class="px-4 py-2 text-[16px] resize-none font-medium transition-all border border-gray-300 rounded-md shadow-sm focus:ring-ring focus:ring-offset-2"
        placeholder="{{ $label }}" name="{{ $name }}" id="{{ $name }}" cols="20" rows="5"></textarea>
</div>
